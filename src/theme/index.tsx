import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#4285F4",
      contrastText: "#fff",
    },
    secondary: {
      main: "#DB4437",
      contrastText: "#fff",
    },
  },
});

export default theme;
